Decisions:
    - New Pane for deleting/renaming/etc?
    - "Create" button that then shows "Save" button after file is created or loaded?
    - JButton interface?

Features To Implement:
    - Settings
    - Better Util Classes
    - Actual Login/Login Database
    - Different Synth L&F's
    - Better Menu Button Logic
    - Inappropriate words or not ?

Features Implemented:
    - Only delete rap pane content if file deleted is one currently loaded
    - Only be able to have one function (Open, Load, Delete) CB open at a time
    - Cancel button to cancel loading / deleting / etc
    - Swing Worker for API Hit
    - Faster API Hit/Parsing

Bugs:
    -

Bugs Fixed:
    - TitleTF doesn't get updated
